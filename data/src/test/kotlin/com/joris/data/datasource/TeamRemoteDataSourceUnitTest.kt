package com.joris.data.datasource


// To unit test remote data source object, we could use a mock server (eg : okhttp mockwebserver) and mock responses
// That way we can ensure encoding is correctly done, and integration testing with repository is possible